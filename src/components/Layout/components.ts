import styled from 'styled-components'

const StyledLayout = styled.div`
  width: 100%;

  /* max-width: 1100px; */

  /* padding: ${({ theme }) => theme.spaces[4]}px; */
  margin: 0 auto;
`
export { StyledLayout }
